import styled from "styled-components";

import { COLOR_BASE } from "@utils/constants";

export const CustomFeedback = styled.div`
  width: 100%;
  margin-top: 0.25rem;
  font-size: 0.875em;
  color: ${COLOR_BASE.ERROR};
  font-size: 0.8rem;
`;
